/** @type {import('tailwindcss').Config} */
export default {
  // Minimal config for IDE IntelliSense only
  // Actual config is in src/app.css via @theme directive (Tailwind v4 CSS-first)
  content: ['./src/**/*.{html,js,svelte,ts}'],
}
